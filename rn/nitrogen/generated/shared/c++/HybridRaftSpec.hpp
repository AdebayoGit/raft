///
/// HybridRaftSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `QueryResult` to properly resolve imports.
namespace margelo::nitro::raft { struct QueryResult; }

#include <string>
#include <NitroModules/Promise.hpp>
#include <optional>
#include "QueryResult.hpp"
#include <functional>

namespace margelo::nitro::raft {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Raft`
   * Inherit this class to create instances of `HybridRaftSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridRaft: public HybridRaftSpec {
   * public:
   *   HybridRaft(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridRaftSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridRaftSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridRaftSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual void open(const std::string& path) = 0;
      virtual std::shared_ptr<Promise<void>> put(const std::string& key, const std::string& value) = 0;
      virtual std::shared_ptr<Promise<std::optional<std::string>>> get(const std::string& key) = 0;
      virtual std::shared_ptr<Promise<std::optional<std::string>>> delete(const std::string& key) = 0;
      virtual void close() = 0;
      virtual std::string watch(const std::string& query, const std::function<void(const QueryResult& /* result */)>& callback) = 0;
      virtual void unwatch(const std::string& subscriptionId) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Raft";
  };

} // namespace margelo::nitro::raft
